namespace Core.Enum
{
    public enum ProjectileDirectionMode
    {
        HorizontalFacing,     // Left / Right only
        UseAttackDirection,   // Free angle
        FixedAngle            // Always X degrees
    }
}